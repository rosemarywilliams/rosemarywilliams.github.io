{
  "name": "rosemary-williams-art-site",
  "private": true,
  "scripts": {
    "build": "wrangler pages functions build --outdir=./dist/worker/",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
